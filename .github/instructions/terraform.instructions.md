---
applyTo: "**/*.tf,**/*.tfvars"
---
# Terraform

- Review plans from `terraform show -json plan.out`; apply the `sre-change-risk-review` skill.
- Flag destroy or replace actions on stateful resources, IAM or role-assignment expansion, and public network exposure.
- Pin provider and module versions. Remote state must use a locked backend.
